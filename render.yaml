# Render Blueprint — deploys the REDLINE multiplayer server.
# In Render: New + > Blueprint, point it at this repo, and it picks this up.
services:
  - type: web
    name: redline-multiplayer
    env: node
    plan: free
    buildCommand: npm install
    startCommand: npm start
    healthCheckPath: /health
    autoDeploy: true
